import React, { ReactElement } from 'react'
import styles from './index.module.less'

const ToPromote = (): ReactElement => {
	return <div className={styles.box}>推广界面</div>
}

export default ToPromote
